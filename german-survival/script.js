const button=document.querySelector('.menu');const rail=document.querySelector('.rail');button?.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')==='true';button.setAttribute('aria-expanded',String(!open));button.textContent=open?'MENU +':'CLOSE ×';});rail?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{button?.setAttribute('aria-expanded','false');if(button)button.textContent='MENU +'}));
